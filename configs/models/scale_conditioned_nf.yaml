backend: learned_field_bundle
evidence_level: learned_model
name: scale-conditioned-nf-reference
family: scale_conditioned_normalizing_flow
seed: 0
readouts:
  - nf_scale_conditioned_fixed
bundle_root: artifacts/field_bundles
artifact_registry: null
artifact_registry_sha256: null
trace:
  backend: exact
  probes: 0
  seed: ${models.seed}
min_finite_fraction: 1.0
